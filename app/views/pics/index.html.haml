// @pics instead of @pic as we want to use all the pictures; @pics defined in pics_controller
//lists each picture


// .col-md-6.col-md-offset-3
// 	#pics.transitions-enabled
// 		- @pics.each do |pic|
// 			.box.panel.panel-default
// 				= link_to (image_tag pic.image.url), pic
// 				%h2= link_to pic.title, pic

.container
  .row
    - @pics.each do |pic|
      #pics.transitions-enabled
        .col-sm-6
          .box.panel.panel-default
            = link_to (image_tag pic.image.url), pic
            %h2
              = link_to pic.title, pic
// = link_to "Gram It", new_pic_path


// - @pics.each do |pic|
//     = link_to (image_tag pic.image.url(:medium)), pic
//     %h2= link_to pic.title, pic 